export default function Page() {
  return (
    <section className="card">
      <h1>Profile / Settings</h1>
      <p>User preferences (diet type, calories goal) and app theme (light/dark).</p>
    </section>
  );
}
